{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"void 0\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "deploy:build": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/admin-web run build",
    "deploy:start": "pnpm --filter @workspace/api-server run start:prod",
    "deploy:migrate": "pnpm --filter @workspace/db run push-force && pnpm --filter @workspace/scripts run seed"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
